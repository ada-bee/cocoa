/** Compatibility export for the upstream server import path. */
export { collectUint8StreamText } from "@t3tools/host-runtime/process";
export type { CollectedUint8StreamText } from "@t3tools/host-runtime/process";
