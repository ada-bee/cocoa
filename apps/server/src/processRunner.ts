/**
 * Compatibility export for the upstream server import path.
 *
 * The implementation is shared with cocoa-hostd through host-runtime so fixes
 * to generic process execution have a single downstream home.
 */
export * from "@t3tools/host-runtime/process";
